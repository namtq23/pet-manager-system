---
phase: 01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/lib/features/auth/repositories/auth_repository.dart
  - frontend/lib/features/auth/controllers/auth_controller.dart
  - frontend/lib/features/auth/views/profile_screen.dart
  - frontend/lib/features/auth/views/change_password_dialog.dart
  - frontend/test/profile_test.dart
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03

estimate:
  tokens: 25000
  raw_tokens: 18000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Supabase Auth automatically handles session persistence and JWT token refresh."
    - "Doctor profile (fullName, phone) can be updated via Supabase Flutter SDK."
    - "Doctor can change password after verifying current password; password must be >= 8 characters."
    - "Doctor with status INACTIVE is blocked from logging in or using session."
  artifacts:
    - "frontend/lib/features/auth/views/profile_screen.dart"
    - "frontend/lib/features/auth/views/change_password_dialog.dart"
  key_links:
    - "ProfileScreen -> AuthRepository -> Supabase database profiles table"

user_setup: []
---

<objective>
Implement Session Persistence, Doctor Profile Management, Password Change, and Account Status Enforcement (`AUTH-03`) using Supabase Auth and Flutter Web.

Purpose: Fulfill profile and password requirements matching SPEC.md sections 3.4 & 3.5 using Supabase client SDK capabilities.
Output: Flutter Profile Screen, Password Change Dialog, Auth State Persistence listener, and automated tests.
</objective>

<execution_context>
@.spec/CLAUDE.md
@.spec/specs/auth/SPEC.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-CONTEXT.md
</execution_context>

<context>
@.spec/CLAUDE.md
@.spec/specs/auth/SPEC.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-CONTEXT.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session Listener, Doctor Profile & Password Management</name>
  <files>
    frontend/lib/features/auth/repositories/auth_repository.dart,
    frontend/lib/features/auth/controllers/auth_controller.dart,
    frontend/lib/features/auth/views/profile_screen.dart,
    frontend/lib/features/auth/views/change_password_dialog.dart
  </files>
  <action>
    1. Update `AuthRepository` to include:
       - `onAuthStateChange` stream listener for automatic session restore.
       - `updateProfile({required String fullName, String? phone})` updating `public.profiles`.
       - `changePassword({required String currentPassword, required String newPassword})`:
         Verify current password via `signInWithPassword` first. If invalid, throw "Mật khẩu hiện tại không đúng". Then call `_client.auth.updateUser(UserAttributes(password: newPassword))`.
    2. Build `ProfileScreen` in Flutter Web: Responsive layout displaying doctor info (fullName, email, phone, role) with Edit Profile capability.
    3. Build `ChangePasswordDialog`: Form validating current password and new password (min 8 chars).
    4. Ensure all functions are kept under 30 lines.
  </action>
  <verify>
    <automated>cd frontend && flutter analyze</automated>
  </verify>
  <done>
    Session listener, Doctor Profile screen, and Change Password dialog implemented cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Automated Tests for Profile & Password Management</name>
  <files>
    frontend/test/profile_test.dart
  </files>
  <action>
    1. Write unit tests in `frontend/test/profile_test.dart`:
       - Profile update payload validation.
       - Password change validation (< 8 chars blocked, wrong current password handled).
       - Inactive account check logic.
    2. Include Traceability Matrix mapping `AUTH-01` & `AUTH-03` requirements at bottom of test file.
  </action>
  <verify>
    <automated>cd frontend && flutter test -- test/profile_test.dart</automated>
  </verify>
  <done>
    Profile management tests with Traceability Matrix passing cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter Client → Supabase Auth | User updating password or profile details |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Password Change | high | mitigate | Re-authenticate with `currentPassword` before updating to `newPassword` |
| T-02-02 | Elevation of Privilege | Profile Update | medium | mitigate | Email field is read-only in UI; RLS policy prevents email mutation by doctor |
</threat_model>

<verification>
- `cd frontend && flutter analyze` passes.
- `cd frontend && flutter test` passes all tests.
</verification>

<success_criteria>
- Doctor can view/edit profile and change password safely.
- Session persistence handled automatically via Supabase Auth.
- All code rules (< 30 lines/function) respected.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-02-SUMMARY.md` when done
</output>
