---
phase: 01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - frontend/lib/features/admin/repositories/admin_user_repository.dart
  - frontend/lib/features/admin/controllers/admin_user_controller.dart
  - frontend/lib/features/admin/views/user_management_screen.dart
  - frontend/lib/features/admin/views/create_doctor_dialog.dart
  - frontend/test/admin_user_mgmt_test.dart
autonomous: true
requirements:
  - AUTH-02

estimate:
  tokens: 25000
  raw_tokens: 18000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Admin can list doctor accounts with status (ACTIVE/INACTIVE)."
    - "Admin can create a new doctor account via Supabase Admin API / Edge Function or DB trigger."
    - "Admin can toggle status (ACTIVE/INACTIVE) of doctor accounts."
    - "Admin cannot disable their own account or the sole remaining ADMIN account."
    - "Non-admin doctors are blocked from accessing Admin screens."
  artifacts:
    - "frontend/lib/features/admin/repositories/admin_user_repository.dart"
    - "frontend/lib/features/admin/views/user_management_screen.dart"
    - "frontend/lib/features/admin/views/create_doctor_dialog.dart"
  key_links:
    - "UserManagementScreen -> AdminUserRepository -> Supabase PostgreSQL profiles table & RLS policies"

user_setup: []
---

<objective>
Implement Doctor Account Management (`AUTH-02`) for Admin users (create doctor, list doctors, toggle active/inactive status, reset password) using Supabase BaaS and Flutter Web UI.

Purpose: Fulfill requirement AUTH-02 for managing 5-15 doctor accounts with Admin role authorization.
Output: Admin User Repository, User Management Screen, Create Doctor Dialog, and automated tests.
</objective>

<execution_context>
@.spec/CLAUDE.md
@.spec/specs/auth/SPEC.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-CONTEXT.md
</execution_context>

<context>
@.spec/CLAUDE.md
@.spec/specs/auth/SPEC.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-CONTEXT.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-01-PLAN.md
@.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin User Repository & User Management Screen</name>
  <files>
    frontend/lib/features/admin/repositories/admin_user_repository.dart,
    frontend/lib/features/admin/controllers/admin_user_controller.dart,
    frontend/lib/features/admin/views/user_management_screen.dart,
    frontend/lib/features/admin/views/create_doctor_dialog.dart
  </files>
  <action>
    1. Implement `AdminUserRepository`:
       - `fetchDoctorUsers()` querying `public.profiles` ordered by `created_at`.
       - `createDoctorUser({required String email, required String fullName, String? phone})`: Create profile entry and invite/create user via Supabase Auth Admin SDK.
       - `toggleUserStatus(String userId, String newStatus)`: Update `status` in `public.profiles`. Check guardrails:
         * Block if `userId == currentUserId` ("Không thể vô hiệu hóa chính tài khoản của bạn").
         * Block if target is last remaining ADMIN ("Cần ít nhất 1 tài khoản Admin trong hệ thống").
    2. Build `UserManagementScreen` in Flutter Web: Responsive data table/cards showing doctors, role, status badge, created date, and action buttons.
    3. Build `CreateDoctorDialog`: Modal form for adding a new doctor.
    4. Guard Admin screen route checking `userProfile.role == 'ADMIN'`. Non-admin doctors are redirected.
    5. Ensure functions are kept under 30 lines.
  </action>
  <verify>
    <automated>cd frontend && flutter analyze</automated>
  </verify>
  <done>
    Admin User Management screen, create dialog, and guardrails implemented cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Automated Tests for Admin Management & Guardrails</name>
  <files>
    frontend/test/admin_user_mgmt_test.dart
  </files>
  <action>
    1. Write tests in `frontend/test/admin_user_mgmt_test.dart`:
       - Verify Admin screen access control for DOCTOR role (blocked).
       - Verify guardrails: self-deactivation blocked, last admin deactivation blocked.
       - Verify Doctor account creation payload.
    2. Append Traceability Matrix for `AUTH-02` at bottom of test file.
  </action>
  <verify>
    <automated>cd frontend && flutter test -- test/admin_user_mgmt_test.dart</automated>
  </verify>
  <done>
    Admin user management test suite with Traceability Matrix passing cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doctor Role → Admin Screen | User with DOCTOR role attempting Admin actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Admin Operations | critical | mitigate | Database RLS policy `is_admin()` verifies ADMIN role server-side; client UI route guard blocks navigation |
| T-03-02 | Denial of Service | Account Deactivation | high | mitigate | Guardrail checks prevent deactivating self or the sole remaining admin |
</threat_model>

<verification>
- `cd frontend && flutter analyze` passes.
- `cd frontend && flutter test` passes all tests.
</verification>

<success_criteria>
- Requirements AUTH-01, AUTH-02, AUTH-03 fully planned and implemented for Flutter Web + Supabase.
- Code simplified to 1 codebase, database-enforced security via RLS policies.
- 30-line function limit respected.
</success_criteria>

<output>
Create `.planning/phases/01-n-n-t-ng-x-c-th-c-b-c-s-foundation-auth/01-03-SUMMARY.md` when done
</output>
