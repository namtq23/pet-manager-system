---
phase: "03"
name: "Upload & Quản lý ảnh theo Lần khám (Photo & Medical Sessions)"
plan_id: "03-01"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - "pet-manager-system/supabase/migrations/20260824200000_create_medical_sessions_and_photos_schema.sql"
  - "pet-manager-system/frontend/pubspec.yaml"
  - "pet-manager-system/frontend/lib/features/photo/models/medical_session.dart"
  - "pet-manager-system/frontend/lib/features/photo/models/medical_photo.dart"
  - "pet-manager-system/frontend/lib/features/photo/repositories/medical_session_repository.dart"
  - "pet-manager-system/frontend/lib/features/photo/repositories/medical_photo_repository.dart"
  - "pet-manager-system/frontend/lib/core/utils/image_compressor.dart"
requirements:
  - PHOTO-01
  - PHOTO-02
must_haves:
  truths:
    - "Bảng public.medical_sessions và public.medical_photos được tạo với RLS policies bảo vệ"
    - "Bucket Storage medical-photos được khởi tạo với RLS policies cho active doctors"
    - "MedicalSessionRepository hỗ trợ tạo và truy vấn danh sách lần khám theo pet_id"
    - "MedicalPhotoRepository hỗ trợ upload file nén lên Storage và lưu metadata vào DB"
  prohibitions:
    - statement: "Không cho phép tài khoản INACTIVE truy cập hoặc upload ảnh"
      status: resolved
      verification: explicit
---

# Plan 03-01: Supabase Migration, Models, Utility & Repositories

## Objective
Xây dựng lớp Nền tảng Dữ liệu & Storage cho Phase 3 gồm: Supabase Migration Script (Bảng `medical_sessions`, `medical_photos`, Bucket `medical-photos` & RLS Policies), Dart Data Models, Utility nén ảnh `ImageCompressor` và 2 Repositories (`MedicalSessionRepository`, `MedicalPhotoRepository`).

## Artifacts this phase produces
- `20260824200000_create_medical_sessions_and_photos_schema.sql`
- `MedicalSession` (Model)
- `MedicalPhoto` (Model)
- `ImageCompressor` (Util)
- `MedicalSessionRepository` (Repo)
- `MedicalPhotoRepository` (Repo)

## Tasks

<task>
<read_first>
- .spec/specs/photo-session/SPEC.md
- pet-manager-system/supabase/migrations/20260824100000_create_customers_and_pets_schema.sql
</read_first>

<acceptance_criteria>
- File `20260824200000_create_medical_sessions_and_photos_schema.sql` tồn tại chứa bảng `medical_sessions`, `medical_photos` và bucket `medical-photos` kèm RLS policies.
</acceptance_criteria>

<action>
Tạo migration SQL `20260824200000_create_medical_sessions_and_photos_schema.sql` định nghĩa bảng `medical_sessions`, `medical_photos`, thiết lập khóa ngoại `ON DELETE CASCADE` và bật RLS policies cho `ACTIVE` doctors.
</action>
</task>

<task>
<read_first>
- pet-manager-system/frontend/pubspec.yaml
- .spec/specs/photo-session/SPEC.md
</read_first>

<acceptance_criteria>
- File `medical_session.dart`, `medical_photo.dart` và `image_compressor.dart` tồn tại với các hàm nén và parse JSON đúng chuẩn.
</acceptance_criteria>

<action>
Thêm package `image_picker` và `image` vào `pubspec.yaml` (nếu cần). Tạo các model Dart `MedicalSession`, `MedicalPhoto` cùng utility `ImageCompressor` để nén ảnh client-side (max 1920px, JPEG 85%).
</action>
</task>

<task>
<read_first>
- pet-manager-system/frontend/lib/features/customer/repositories/pet_repository.dart
- .spec/specs/photo-session/SPEC.md
</read_first>

<acceptance_criteria>
- `MedicalSessionRepository` và `MedicalPhotoRepository` được viết hoàn chỉnh, các hàm ngắn hơn 30 dòng, hỗ trợ CRUD đợt khám và upload file storage.
</acceptance_criteria>

<action>
Viết `MedicalSessionRepository` và `MedicalPhotoRepository` tương tác với Supabase Database & Storage bucket `medical-photos`. Tuân thủ quy tắc < 30 dòng mỗi hàm.
</action>
</task>

## Verification
- Chạy static analysis / build check trên Flutter để đảm bảo repositories & models biên dịch thành công không lỗi syntax.
