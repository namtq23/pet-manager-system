---
phase: "03"
name: "Upload & Quản lý ảnh theo Lần khám (Photo & Medical Sessions)"
plan_id: "03-02"
type: "expansion"
wave: 2
depends_on: ["03-01"]
files_modified:
  - "pet-manager-system/frontend/lib/features/photo/controllers/medical_session_controller.dart"
  - "pet-manager-system/frontend/lib/features/photo/controllers/photo_upload_controller.dart"
  - "pet-manager-system/frontend/lib/features/photo/views/widgets/medical_session_card.dart"
  - "pet-manager-system/frontend/lib/features/photo/views/widgets/photo_gallery_grid.dart"
  - "pet-manager-system/frontend/lib/features/photo/views/widgets/photo_lightbox_viewer.dart"
  - "pet-manager-system/frontend/lib/features/photo/views/dialogs/create_session_dialog.dart"
  - "pet-manager-system/frontend/lib/features/photo/views/pet_medical_history_tab.dart"
requirements:
  - PHOTO-01
  - PHOTO-02
  - PHOTO-03
must_haves:
  truths:
    - "MedicalSessionController quản lý danh sách các đợt khám của cún với trạng thái loading/error"
    - "PhotoUploadController hỗ trợ chọn ảnh từ camera/thư viện và xử lý upload nhiều ảnh đồng thời"
    - "Giao diện hiển thị đợt khám theo thứ tự thời gian mới nhất lên đầu"
    - "Hỗ trợ xem ảnh phóng to full screen bằng Lightbox Viewer"
  prohibitions:
    - statement: "Không được bỏ qua nén ảnh client-side khi upload ảnh từ thiết bị"
      status: resolved
      verification: explicit
---

# Plan 03-02: Controllers & UI Views (Session List, Upload Dialog & Lightbox Viewer)

## Objective
Xây dựng lớp Controllers (Riverpod StateNotifier) và giao diện UI hoàn chỉnh cho Phase 3 gồm: danh sách Lần khám theo thứ tự thời gian (`PetMedicalHistoryTab`), modal tạo Lần khám & upload ảnh (`CreateSessionDialog`), lưới xem ảnh (`PhotoGalleryGrid`) và trình xem ảnh phóng to toàn màn hình (`PhotoLightboxViewer`).

## Artifacts this phase produces
- `MedicalSessionController`
- `PhotoUploadController`
- `PetMedicalHistoryTab`
- `MedicalSessionCard`
- `CreateSessionDialog`
- `PhotoGalleryGrid`
- `PhotoLightboxViewer`

## Tasks

<task>
<read_first>
- pet-manager-system/frontend/lib/features/customer/controllers/pet_controller.dart
- .spec/specs/photo-session/SPEC.md
</read_first>

<acceptance_criteria>
- File `medical_session_controller.dart` và `photo_upload_controller.dart` được định nghĩa chuẩn Riverpod notifier, quản lý việc load danh sách đợt khám và upload ảnh.
</acceptance_criteria>

<action>
Tạo `MedicalSessionController` (fetch sessions theo `pet_id`, create/delete session) và `PhotoUploadController` (handle picker & nén & batch upload). Hàm < 30 dòng.
</action>
</task>

<task>
<read_first>
- .spec/specs/photo-session/SPEC.md
- pet-manager-system/frontend/lib/features/photo/repositories/medical_photo_repository.dart
</read_first>

<acceptance_criteria>
- File `photo_gallery_grid.dart` và `photo_lightbox_viewer.dart` tạo giao diện xem ảnh lưới và phóng to mượt mà trên cả Mobile Web và PC.
</acceptance_criteria>

<action>
Tạo widget `PhotoGalleryGrid` (hiển thị thumbnail ảnh kèm caption) và `PhotoLightboxViewer` (bấm vào thumbnail mở dialog xem full screen).
</action>
</task>

<task>
<read_first>
- pet-manager-system/frontend/lib/features/customer/views/customer_detail_screen.dart
- .spec/specs/photo-session/SPEC.md
</read_first>

<acceptance_criteria>
- `CreateSessionDialog` và `PetMedicalHistoryTab` tạo thành công, tích hợp vào màn hình chi tiết cún.
</acceptance_criteria>

<action>
Tạo `CreateSessionDialog` (form nhập tiêu đề, chẩn đoán, ghi chú + nút chọn/chụp ảnh) và `PetMedicalHistoryTab` (danh sách đợt khám mới nhất lên đầu).
</action>
</task>

## Verification
- Chạy static code analysis (`flutter analyze` hoặc build check) đảm bảo các widgets UI và controllers biên dịch không có lỗi.
