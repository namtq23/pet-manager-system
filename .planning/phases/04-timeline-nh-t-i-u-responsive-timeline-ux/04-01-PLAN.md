---
wave: 1
depends_on: []
files_modified:
  - frontend/lib/features/photo/controllers/timeline_controller.dart
  - frontend/lib/features/photo/models/comparison_pair.dart
autonomous: true
---

# Plan 04-01: Timeline & Comparison Controller Setup (Tracer Slice)

## Objective
Xây dựng Model & Controller quản lý trạng thái chọn ảnh so sánh Before/After và lọc tiến trình điều trị theo thời gian của thú cưng (`TimelineController`).

## Tasks

<task>
  <read_first>
    - frontend/lib/features/photo/controllers/medical_session_controller.dart
    - frontend/lib/features/photo/models/medical_photo.dart
    - .spec/CLAUDE.md
  </read_first>
  <acceptance_criteria>
    - File `frontend/lib/features/photo/models/comparison_pair.dart` được tạo chứa model `ComparisonPair` (gồm `beforePhoto` và `afterPhoto`).
    - File `frontend/lib/features/photo/controllers/timeline_controller.dart` được tạo chứa `TimelineController` kế thừa `ChangeNotifier`.
    - `TimelineController` hỗ trợ các phương thức: `selectBeforePhoto()`, `selectAfterPhoto()`, `clearSelection()`, `isComparisonReady()`.
    - Mọi hàm trong file < 30 dòng code theo chuẩn `.spec/CLAUDE.md`.
    - Có comment tag EARS: `// EARS[Event]: WHEN user selects photos for comparison...`
  </acceptance_criteria>
  <action>
    Tạo model `ComparisonPair` lưu 2 ảnh `beforePhoto` và `afterPhoto`.
    Tạo `TimelineController` để quản lý việc chọn 2 ảnh từ các đợt khám khác nhau hoặc cùng 1 đợt khám.
  </action>
  <verify>
    Chạy `flutter test` hoặc kiểm tra code syntax để đảm bảo controller khởi tạo thành công không có lỗi compile.
  </verify>
</task>

## Verification
- Model & Controller compile thành công.
- Trạng thái chọn ảnh được cập nhật chính xác khi gọi phương thức.

## must_haves
- truths:
  - "TimelineController quản lý chính xác việc chọn ảnh Before và After"
  - "ComparisonPair lưu giữ cặp ảnh cần so sánh cùng metadata"
