---
wave: 2
depends_on: ["04-01-PLAN.md"]
files_modified:
  - frontend/lib/features/photo/views/widgets/timeline_node_widget.dart
  - frontend/lib/features/photo/views/widgets/before_after_comparison_viewer.dart
  - frontend/lib/features/photo/views/pet_timeline_screen.dart
  - frontend/lib/features/customer/views/customer_detail_screen.dart
autonomous: true
---

# Plan 04-02: Timeline & Before/After Comparison UI Views

## Objective
Xây dựng giao diện Timeline dọc (`PetTimelineScreen`), widget so sánh ảnh Before/After (`BeforeAfterComparisonViewer`), và tích hợp vào màn hình Chi tiết Khách hàng / Thú cưng (`CustomerDetailScreen`).

## Tasks

<task>
  <read_first>
    - frontend/lib/features/photo/controllers/timeline_controller.dart
    - frontend/lib/features/photo/views/pet_medical_history_tab.dart
    - frontend/lib/features/customer/views/customer_detail_screen.dart
    - .spec/CLAUDE.md
  </read_first>
  <acceptance_criteria>
    - File `frontend/lib/features/photo/views/widgets/timeline_node_widget.dart` hiển thị đường nối dọc và mốc thời gian đợt khám.
    - File `frontend/lib/features/photo/views/widgets/before_after_comparison_viewer.dart` hỗ trợ 2 chế độ so sánh: Side-by-Side (Song song) và Interactive Slider Overlay (Kéo trượt).
    - File `frontend/lib/features/photo/views/pet_timeline_screen.dart` hiển thị toàn bộ tiến trình đợt khám dạng Timeline và nút kích hoạt xem So sánh ảnh.
    - Màn hình `CustomerDetailScreen` có tab/nút truy cập vào Timeline ảnh của cún.
    - Responsive layout: Tự động điều chỉnh giao diện phù hợp với Mobile Web (<600px) và Desktop Web (≥600px).
    - Mọi hàm UI < 30 dòng code theo chuẩn `.spec/CLAUDE.md`.
  </acceptance_criteria>
  <action>
    Xây dựng `TimelineNodeWidget` bọc `MedicalSessionCard`.
    Xây dựng `BeforeAfterComparisonViewer` cho phép kéo thanh slider trượt qua lại giữa 2 ảnh.
    Tạo `PetTimelineScreen` hiển thị toàn bộ đợt khám dạng Timeline.
    Tích hợp Tab "Timeline Tiến Trình" vào `CustomerDetailScreen`.
  </action>
  <verify>
    Kiểm tra render UI không bịoverflow trên cả Mobile Web và PC Web.
  </verify>
</task>

## Verification
- Giao diện Timeline hiển thị mượt mà.
- Thanh trượt so sánh ảnh Before/After kéo mượt mà, đổi qua lại giữa 2 ảnh chính xác.

## must_haves
- truths:
  - "PetTimelineScreen hiển thị đúng các đợt khám theo mốc thời gian"
  - "BeforeAfterComparisonViewer cho phép so sánh ảnh dạng Slider và Side-by-Side"
  - "Giao diện tương thích hoàn hảo trên Mobile và PC"
