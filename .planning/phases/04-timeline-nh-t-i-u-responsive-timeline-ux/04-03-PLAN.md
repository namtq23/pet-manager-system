---
wave: 3
depends_on: ["04-01-PLAN.md", "04-02-PLAN.md"]
files_modified:
  - frontend/test/timeline_controller_test.dart
  - frontend/test/timeline_widget_test.dart
  - frontend/test/responsive_layout_test.dart
autonomous: true
---

# Plan 04-03: Verification, Automated Tests & Responsive Audit

## Objective
Xây dựng bộ kiểm thử tự động (Unit, Widget tests) cho các tính năng Timeline, So sánh ảnh Before/After và Responsive layout, đảm bảo độ tin cậy và tuân thủ các quy chuẩn trong `.spec/CLAUDE.md`.

## Tasks

<task>
  <read_first>
    - frontend/lib/features/photo/controllers/timeline_controller.dart
    - frontend/lib/features/photo/views/pet_timeline_screen.dart
    - frontend/test/auth_test.dart
    - .spec/CLAUDE.md
  </read_first>
  <acceptance_criteria>
    - File `frontend/test/timeline_controller_test.dart` verify các phương thức chọn ảnh Before/After, reset và trạng thái so sánh.
    - File `frontend/test/timeline_widget_test.dart` verify hiển thị đợt khám trên Timeline và màn hình so sánh Before/After.
    - File `frontend/test/responsive_layout_test.dart` verify giao diện thay đổi thích ứng trên Mobile (<600px) và Desktop (≥600px).
    - Cuối mỗi file test có Ma trận kiểm thử Traceability Matrix ánh xạ requirement `PHOTO-04`.
    - Mọi hàm test < 30 dòng code theo chuẩn `.spec/CLAUDE.md`.
  </acceptance_criteria>
  <action>
    Tạo các file test kiểm thử Unit và Widget.
    Bổ sung Traceability Matrix ở cuối mỗi file test.
  </action>
  <verify>
    Chạy `flutter test` đảm bảo 100% test cases pass.
  </verify>
</task>

## Verification
- All tests pass.
- Ma trận kiểm thử phủ kín requirement PHOTO-04.

## must_haves
- truths:
  - "Tất cả unit test & widget test cho Timeline & Responsive UX chạy thành công 100%"
  - "Ma trận kiểm thử Traceability Matrix trong test file đầy đủ"
